@model Plan

<h2>Detalle del Plan: @Model.Nombre</h2>

<p>Actividades:</p>
<ul>
    @foreach (var a in Model.Actividades)
    {
        <li>@a.Nombre - @a.Duracion min - $@a.Precio</li>
    }
</ul>

<a asp-action="AgregarActividad" asp-route-planId="@Model.Id">Agregar Actividad</a>